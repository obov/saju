{
  "name": "back",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "AWS_PROFILE=saju AWS_REGION=ap-northeast-2 sam local start-api",
    "build": "sam build",
    "deploy": "AWS_PROFILE=saju AWS_DEFAULT_REGION=ap-northeast-2 sam deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.535.0",
    "@aws-sdk/s3-request-presigner": "^3.705.0"
  }
}
